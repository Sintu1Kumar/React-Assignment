import React from 'react'

export default function Assignment_1() {
  return (
    <div>
      <h1>React Assignment-1</h1>
      Q1. What is Virtual DOM?
      Ans:-Document Object Model that represents the content of XML or HTML documents as a tree structure so that the programs can be read, accessed and changed in the document structure, style, and content.

      Q2. What is SPA?
      Ans:- Reat is a single page application. react allows to use other page without loading whole pages.

      Q3. What is difference between class and functional component?
      And:- Class Component - A class component requires you to extend from React. Component and create a render function that returns a React element. it is used to react life cycle method.

      Functional component - A functional component is a JavaScript function that accepts props as an argument and returns a React element. it is not used the react life cycle method.

      Q4. What does mean by state and its use in react?
      Ans:- The state is a built-in React object that is store data about the component. When a value in the state object changes, the output will change according to the new data.

      Q5. What is JSX and why do we use it instead of js?
      Ans:- jsx is the JavaScript XML(syntax extension). it is used to react JS because of it suport HTML and javascript both syntex so it provides easy way to write HTML code in React.

      Q6. What is package.json?
      Ans:- package json file is to store the install all package name and its verson it is also provide the information about dependency package.

      Q7. What is the Difference between react and react native? which one is library or framework?
      Ans:- Reactjs is the labuary but react native is the framework it is also used to create mobile application.

      Q8. What is the package name you are using for routing?
      Ans:- package name- react-router-DOM
      
    </div>
  )
}

